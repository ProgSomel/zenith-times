const elementById = (id) => {
    const element = document.getElementById(id);
    return element;
}

const elementCreate = (type) => {
    const element = document.createElement(type);
    return element;
}

